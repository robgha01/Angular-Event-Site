@*@using SpecialistHjalpen.Web.Controllers
@inherits UmbracoViewPage<SpecialistHjalpen.Web.ViewModels.RegisterViewModel>

@if (!User.Identity.IsAuthenticated)
{
    using (Html.BeginUmbracoForm<LoginRegisterController>("Register", FormMethod.Post, new { @class = "form", role = "form" }))
    {
        @Html.ValidationSummary(false)
        @Html.AntiForgeryToken()

        <div class="form-group">
            @Html.LabelFor(m => m.UserName)
            @Html.TextBoxFor(m => m.UserName, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.UserName)
        </div>

        <div class="form-group">
            @Html.LabelFor(m => m.Password)
            @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Password)
        </div>

        <div class="form-group">
            @Html.LabelFor(m => m.Email)
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Email)
        </div>

        <div class="submit-holder">
            <button type="submit" class="btn btn-default">
                @Umbraco.GetDictionaryValue("Buttons.Register")
            </button>
        </div>

        @Html.HiddenFor(x => x.ReturnUrl)
    }

}
else
{
    <p>@Umbraco.GetDictionaryValue("Members.AlreadyRegistered")</p>
}*@
