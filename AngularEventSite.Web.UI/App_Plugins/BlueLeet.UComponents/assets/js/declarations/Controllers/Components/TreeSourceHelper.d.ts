import { TreeSourceType } from "./TreeSourceType";
export declare class TreeSourceHelper {
    static parseSourceType(value: any): TreeSourceType;
    static getEntityType(sourceType: TreeSourceType): string;
}
